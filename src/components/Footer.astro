---
// Contact placeholder — edit the location (and add links) here.
---

<footer>
  <span>📍 Metro Manila, Philippines</span>
</footer>

<style>
  footer {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: calc(var(--space) * 2) var(--space);
    border-top: 1px solid var(--border);
    color: var(--muted);
    font-size: 0.9em;
  }
</style>
