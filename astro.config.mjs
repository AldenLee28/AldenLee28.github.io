// @ts-check
import { defineConfig } from 'astro/config';

// User site served at the root URL — no `base` needed.
// https://astro.build/config
export default defineConfig({
  site: 'https://aldenlee28.github.io',
});
